// Variáveis do console
command_input = "";        // Texto que o usuário está digitando
is_open = false;           // Console aberto/fechado
command_history = [];      // Histórico de comandos (opcional)
history_index = 0;         // Para navegar no histórico
max_history = 50;          // Limite do histórico

max_debug_text=20
debug_text=[]

global.cap=0 //teste

/*
// Inicializa a variável global
if (is_undefined(global.cap)) {
    global.cap = 0;
}
*/
